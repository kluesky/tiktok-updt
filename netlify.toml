[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Skip TypeScript error
[context.production.environment]
  CI = false
  NPM_FLAGS = "--no-audit --no-fund"